The params.txt file in builds folder specifies parameters for various position tabs in main vi (hidden but can be seen if scrolled up)

The orderis as below:

Exposure Time (ms), Gain, Contrast, Gamma

There are 4 rows, 1 for each position

Fifth row is zooms for various zoom tabs (also hidden but can be seen by scrolling up)